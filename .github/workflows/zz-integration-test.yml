name: Integration Tests

on:
  workflow_call:

env:
  REDGATE_DISABLE_TELEMETRY: 'true'

jobs:
  list:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Run root action
        id: list
        uses: ./
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Actions: ${{ steps.list.outputs.actions }}"
          echo '${{ steps.list.outputs.actions }}' | jq -e '. | index("migrations/checks") != null'
          echo '${{ steps.list.outputs.actions }}' | jq -e '. | index("migrations/deploy") != null'
          echo '${{ steps.list.outputs.actions }}' | jq -e '. | index("migrations/undo") != null'
          echo '${{ steps.list.outputs.actions }}' | jq -e '. | index("state/deploy") != null'
          echo '${{ steps.list.outputs.actions }}' | jq -e '. | index("state/prepare") != null'

  migrations-checks:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run checks
        id: checks
        uses: ./migrations/checks
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/no-drift.db
          build-url: jdbc:sqlite:database/build.db
          build-ok-to-erase: 'true'
          fail-on-code-review: 'false'
          working-directory: sample-projects/sqlite
          pre-deployment-report-name: flyway-pre-deployment-report-${{ matrix.os }}
          pre-deployment-report-retention-days: '1'
      - name: Download report
        uses: actions/download-artifact@v8
        with:
          name: flyway-pre-deployment-report-${{ matrix.os }}.html
      - name: Verify report exists
        shell: bash
        run: test -s flyway-pre-deployment-report-${{ matrix.os }}.html
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.checks.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.checks.outputs.drift-detected }}"
          echo "Changed object count: ${{ steps.checks.outputs.changed-object-count }}"
          echo "Code violation count: ${{ steps.checks.outputs.code-violation-count }}"
          echo "Code violation codes: ${{ steps.checks.outputs.code-violation-codes }}"
          test "${{ steps.checks.outputs.exit-code }}" = "0"
          test "${{ steps.checks.outputs.drift-detected }}" = "false"
          test "${{ steps.checks.outputs.changed-object-count }}" = "2"
          test "${{ steps.checks.outputs.code-violation-count }}" = "1"
          test "${{ steps.checks.outputs.code-violation-codes }}" = "AM04"

  migrations-checks-fail-on-drift-true:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run checks
        id: checks
        uses: ./migrations/checks
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/drift.db
          fail-on-code-review: 'false'
          working-directory: sample-projects/sqlite
          pre-deployment-report-name: flyway-drift-report-fail-true-${{ matrix.os }}
          pre-deployment-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-drift-resolution-scripts-fail-true-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Verify checks failed due to drift
        shell: bash
        run: test "${{ steps.checks.outcome }}" != "success"
      - name: Download report
        uses: actions/download-artifact@v8
        with:
          name: flyway-drift-report-fail-true-${{ matrix.os }}.html
      - name: Verify report exists
        shell: bash
        run: test -s flyway-drift-report-fail-true-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-drift-resolution-scripts-fail-true-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.checks.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.checks.outputs.drift-detected }}"
          test "${{ steps.checks.outputs.exit-code }}" = "1"
          test "${{ steps.checks.outputs.drift-detected }}" = "true"

  migrations-checks-fail-on-drift-false:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run checks
        id: checks
        uses: ./migrations/checks
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/drift.db
          fail-on-drift: 'false'
          fail-on-code-review: 'false'
          working-directory: sample-projects/sqlite
          pre-deployment-report-name: flyway-drift-report-fail-false-${{ matrix.os }}
          pre-deployment-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-drift-resolution-scripts-fail-false-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Download report
        uses: actions/download-artifact@v8
        with:
          name: flyway-drift-report-fail-false-${{ matrix.os }}.html
      - name: Verify report exists
        shell: bash
        run: test -s flyway-drift-report-fail-false-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-drift-resolution-scripts-fail-false-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.checks.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.checks.outputs.drift-detected }}"
          test "${{ steps.checks.outputs.exit-code }}" = "0"
          test "${{ steps.checks.outputs.drift-detected }}" = "true"

  migrations-checks-fail-on-code-review:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run checks
        id: checks
        uses: ./migrations/checks
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/no-drift.db
          working-directory: sample-projects/sqlite
          skip-pre-deployment-report-upload: 'true'
      - name: Verify checks failed due to code review
        shell: bash
        run: test "${{ steps.checks.outcome }}" != "success"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.checks.outputs.exit-code }}"
          echo "Code violation count: ${{ steps.checks.outputs.code-violation-count }}"
          echo "Code violation codes: ${{ steps.checks.outputs.code-violation-codes }}"
          test "${{ steps.checks.outputs.exit-code }}" = "1"
          test "${{ steps.checks.outputs.code-violation-count }}" = "1"
          test "${{ steps.checks.outputs.code-violation-codes }}" = "AM04"

  migrations-deploy-no-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run migrations deploy
        id: deploy
        uses: ./migrations/deploy
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/no-drift.db
          working-directory: sample-projects/sqlite
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.deploy.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.deploy.outputs.drift-detected }}"
          echo "Migrations applied: ${{ steps.deploy.outputs.migrations-applied }}"
          echo "Schema version: ${{ steps.deploy.outputs.schema-version }}"
          test "${{ steps.deploy.outputs.exit-code }}" = "0"
          test "${{ steps.deploy.outputs.drift-detected }}" = "false"
          test "${{ steps.deploy.outputs.migrations-applied }}" = "3"
          test "${{ steps.deploy.outputs.schema-version }}" = "3"

  migrations-deploy-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run migrations deploy
        id: deploy
        uses: ./migrations/deploy
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/drift.db
          working-directory: sample-projects/sqlite
          drift-report-name: flyway-deploy-drift-report-${{ matrix.os }}
          drift-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-deploy-drift-resolution-scripts-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Verify deploy failed due to drift
        shell: bash
        run: test "${{ steps.deploy.outcome }}" != "success"
      - name: Download drift report
        uses: actions/download-artifact@v8
        with:
          name: flyway-deploy-drift-report-${{ matrix.os }}.html
      - name: Verify drift report exists
        shell: bash
        run: test -s flyway-deploy-drift-report-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-deploy-drift-resolution-scripts-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.deploy.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.deploy.outputs.drift-detected }}"
          echo "Migrations applied: ${{ steps.deploy.outputs.migrations-applied }}"
          echo "Schema version: ${{ steps.deploy.outputs.schema-version }}"
          test "${{ steps.deploy.outputs.exit-code }}" = "1"
          test "${{ steps.deploy.outputs.drift-detected }}" = "true"
          test "${{ steps.deploy.outputs.migrations-applied }}" = ""
          test "${{ steps.deploy.outputs.schema-version }}" = ""

  migrations-undo-no-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run migrations undo
        id: undo
        uses: ./migrations/undo
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/undo.db
          working-directory: sample-projects/sqlite
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.undo.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.undo.outputs.drift-detected }}"
          echo "Migrations undone: ${{ steps.undo.outputs.migrations-undone }}"
          echo "Schema version: ${{ steps.undo.outputs.schema-version }}"
          test "${{ steps.undo.outputs.exit-code }}" = "0"
          test "${{ steps.undo.outputs.drift-detected }}" = "false"
          test "${{ steps.undo.outputs.migrations-undone }}" = "1"
          test "${{ steps.undo.outputs.schema-version }}" = "2"

  migrations-undo-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run migrations undo
        id: undo
        uses: ./migrations/undo
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/drift.db
          working-directory: sample-projects/sqlite
          drift-report-name: flyway-undo-drift-report-${{ matrix.os }}
          drift-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-undo-drift-resolution-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Download drift report
        uses: actions/download-artifact@v8
        with:
          name: flyway-undo-drift-report-${{ matrix.os }}.html
      - name: Verify drift report exists
        shell: bash
        run: test -s flyway-undo-drift-report-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-undo-drift-resolution-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Verify undo failed due to drift
        shell: bash
        run: test "${{ steps.undo.outcome }}" != "success"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.undo.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.undo.outputs.drift-detected }}"
          echo "Migrations undone: ${{ steps.undo.outputs.migrations-undone }}"
          echo "Schema version: ${{ steps.undo.outputs.schema-version }}"
          test "${{ steps.undo.outputs.exit-code }}" = "1"
          test "${{ steps.undo.outputs.drift-detected }}" = "true"
          test "${{ steps.undo.outputs.migrations-undone }}" = ""
          test "${{ steps.undo.outputs.schema-version }}" = ""

  state-prepare-no-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run state prepare
        id: prepare
        uses: ./state/prepare
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-environment: production
          target-url: jdbc:sqlite:state-prepare-target.db
          working-directory: sample-projects/sqlite-state
          skip-code-review: 'true'
      - name: Test prepare outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.prepare.outputs.exit-code }}"
          echo "Script path: ${{ steps.prepare.outputs.script-path }}"
          echo "Undo script path: ${{ steps.prepare.outputs.undo-script-path }}"
          echo "Changed object count: ${{ steps.prepare.outputs.changed-object-count }}"
          test "${{ steps.prepare.outputs.exit-code }}" = "0"
          [[ "${{ steps.prepare.outputs.script-path }}" == *D__deployment.sql ]]
          [[ "${{ steps.prepare.outputs.undo-script-path }}" == *DU__undo.sql ]]
          test "${{ steps.prepare.outputs.changed-object-count }}" = "3"

  state-prepare-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run state prepare with drift
        id: prepare
        uses: ./state/prepare
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-environment: production
          target-url: jdbc:sqlite:../sqlite/database/drift.db
          working-directory: sample-projects/sqlite-state
          skip-code-review: 'true'
          pre-deployment-report-name: flyway-state-prepare-pre-deployment-report-${{ matrix.os }}
          pre-deployment-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-state-prepare-drift-resolution-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Download pre deployment report
        uses: actions/download-artifact@v8
        with:
          name: flyway-state-prepare-pre-deployment-report-${{ matrix.os }}.html
      - name: Verify pre-deployment report exists
        shell: bash
        run: test -s flyway-state-prepare-pre-deployment-report-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-state-prepare-drift-resolution-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Verify prepare failed due to drift
        shell: bash
        run: test "${{ steps.prepare.outcome }}" != "success"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.prepare.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.prepare.outputs.drift-detected }}"
          test "${{ steps.prepare.outputs.exit-code }}" = "1"
          test "${{ steps.prepare.outputs.drift-detected }}" = "true"

  state-prepare-fail-on-code-review:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run state prepare with code review
        id: prepare
        uses: ./state/prepare
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-environment: production
          target-url: jdbc:sqlite:state-prepare-target.db
          working-directory: sample-projects/sqlite-state
          skip-drift-check: 'true'
      - name: Verify prepare failed due to code review
        shell: bash
        run: test "${{ steps.prepare.outcome }}" != "success"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.prepare.outputs.exit-code }}"
          echo "Changed object count: ${{ steps.prepare.outputs.changed-object-count }}"
          echo "Code violation count: ${{ steps.prepare.outputs.code-violation-count }}"
          echo "Code violation codes: ${{ steps.prepare.outputs.code-violation-codes }}"
          test "${{ steps.prepare.outputs.exit-code }}" = "1"
          test "${{ steps.prepare.outputs.changed-object-count }}" = "3"
          test "${{ steps.prepare.outputs.code-violation-count }}" = "1"
          test "${{ steps.prepare.outputs.code-violation-codes }}" = "AM04"

  state-deploy-no-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run state deploy
        id: deploy
        uses: ./state/deploy
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:state-target.db
          script-path: deployments/D__deployment.sql
          working-directory: sample-projects/sqlite
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.deploy.outputs.exit-code }}"
          test "${{ steps.deploy.outputs.exit-code }}" = "0"

  state-deploy-drift:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Retrieve secrets from Vault
        uses: hashicorp/vault-action@v3.4.0
        with:
          method: jwt
          path: ${{ vars.VAULT_AUTH_PATH }}
          url: ${{ vars.VAULT_URL }}
          role: "flyway-actions"
          secrets: |
            build-secrets/data/repos/red-gate/flyway-actions/flyway FLYWAY_PERMIT_ENTERPRISE | FLYWAY_PERMIT_ENTERPRISE;
      - name: Setup Flyway
        uses: red-gate/setup-flyway@v3
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          edition: enterprise
          i-agree-to-the-eula: true
      - name: Run state deploy with drift
        id: deploy
        uses: ./state/deploy
        continue-on-error: true
        env:
          REDGATE_LICENSING_PERMIT: ${{ env.FLYWAY_PERMIT_ENTERPRISE }}
        with:
          target-url: jdbc:sqlite:database/drift.db
          script-path: deployments/D__deployment.sql
          working-directory: sample-projects/sqlite
          drift-report-name: flyway-state-deploy-drift-report-${{ matrix.os }}
          drift-report-retention-days: '1'
          drift-resolution-scripts-name: flyway-state-deploy-drift-resolution-${{ matrix.os }}
          drift-resolution-scripts-retention-days: '1'
      - name: Download drift report
        uses: actions/download-artifact@v8
        with:
          name: flyway-state-deploy-drift-report-${{ matrix.os }}.html
      - name: Verify drift report exists
        shell: bash
        run: test -s flyway-state-deploy-drift-report-${{ matrix.os }}.html
      - name: Download drift resolution scripts
        uses: actions/download-artifact@v8
        with:
          name: flyway-state-deploy-drift-resolution-${{ matrix.os }}
      - name: Verify drift resolution scripts exist
        shell: bash
        run: test -n "$(find . -maxdepth 1 -name '*.sql')"
      - name: Verify deploy failed due to drift
        shell: bash
        run: test "${{ steps.deploy.outcome }}" != "success"
      - name: Test outputs
        shell: bash
        run: |
          set -euo pipefail
          echo "Exit code: ${{ steps.deploy.outputs.exit-code }}"
          echo "Drift detected: ${{ steps.deploy.outputs.drift-detected }}"
          test "${{ steps.deploy.outputs.exit-code }}" = "1"
          test "${{ steps.deploy.outputs.drift-detected }}" = "true"
