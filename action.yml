name: 'Redgate Flyway GitHub Actions'
description: 'Official Redgate Actions: Set up, check, and deploy changes with Redgate Flyway'
author: 'Redgate'

branding:
  icon: 'database'
  color: 'red'

outputs:
  actions:
    description: 'JSON array of available actions'

runs:
  using: 'node24'
  main: 'list/dist/index.js'
